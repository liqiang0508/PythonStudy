{
  "name": "electrontest",
  "version": "1.0.0",
  "description": "My Electron app",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager ./ hotMusic --platform=win32 --arch=x64 --icon=./icon.ico --out=./out --electron-version=11.1.0 --app-version=0.1.0 --overwrite --ignore=node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@electron-forge/cli": "^6.0.0-beta.59",
    "electron": "^11.1.0",
    "electron-packager": "^15.3.0"
  }
}
